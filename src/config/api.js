/**
 * API Configuration
 *
 * Use relative URLs in all environments:
 * - Development: Proxied through Vite dev server (localhost:5173/api -> localhost:3001/api)
 * - Production: Same origin (both served from same host)
 *
 * This ensures cookies work correctly as all requests appear to come from the same origin.
 */

const API_BASE_URL = ''; // Always use relative URLs (empty string prefix)

export default API_BASE_URL;
